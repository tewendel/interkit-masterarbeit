# Docs

## Content Files

Docs consist of files that can be in the formats
- markdown (`.md`)
- mdsvex (`.svx` or `.svelte.md`)
- svelte (`.svelte`)

Files are in subdirectories in `docs/src/routes` 

## Sidebar

Sidebar is configured manually in `docs/src/sidebar.json.js`

