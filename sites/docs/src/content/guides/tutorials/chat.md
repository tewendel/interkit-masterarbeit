# Interactive Stories and Chatbots

The node editor provides four ways to work on behaviour.

You use the Javscript API in the `Handlers` and the `Full` tab. 

In the the `Strings` tab, you can only edit the text parts of the code. This is useful, if you are not 
